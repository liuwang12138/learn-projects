package com.gf.algorithm.class04;

/**
 *
 *  与堆有关的题目：
 *  	一直一个几乎有序的数组，几乎有序是指，如果把数组排好顺序的话，每个元素移动的距离一定不超过k，并且k相对于数组长度来说是比较小的
 *  	请选择一个合适的排序策略，对这个数组进行排序
 *		
 *	建一个heapSize = k的小根堆，放入k个数，弹出一个，放在0位置，再放一个，弹出一个，放在1位置...
 *
 */
public class G02_SortArrayDistanceLessK {

}
